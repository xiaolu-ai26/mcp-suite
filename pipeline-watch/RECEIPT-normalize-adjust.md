# 收据：公司名规范化按站长口径收窄——只补空，不合并品牌

- 分支：`feat/normalize-adjust`（worktree `/Volumes/臭垃圾桶/生财MCP/_worktrees/normalize-adjust`，
  基线 `feat/collector-next-4` `82af4f72`）
- 状态：**代码 + 单测 + 精灵临时目录全库干跑完成；未部署、未覆盖精灵正式目录、未碰阿里云、
  未调飞书、未读取/打印令牌、未登录、未 push、未合并 main、未终止任何进程**
- 部署件：`pipeline-watch/deploy-artifacts/20260919i/`（在 `20260919g` 基础上**只替换 2 个同名文件**）
- 干跑目录（精灵）：`C:\mcp-suite-normalize-test\`（正式目录 `C:\mcp-suite-collector` 全程只读）

---

## 1. 先结论

1. **站长口径落成**：不做任何“把独立品牌/子品牌/子公司/省分公司/事业部并入母公司”的合并；
   只做**同一实体的不同写法**归一（剥法人后缀、品牌简称），拿不准保留原名。
2. **别名表删 5 条合并映射**：2 条品牌并入（`网易互娱 → 网易`、`网易互联网 → 网易`）
   + 3 条前缀式集团合并（原 `prefixes`：中国移动通信 / 中国联合网络通信 / 中国邮政）。
   新增 2 条**集团法人自己**的精确别名（`中国移动通信集团有限公司 → 中国移动`、
   `中国联合网络通信集团有限公司 → 中国联通`）；`company_names.py` 的 `prefixes` 分支整体删除。
3. **“补空”照旧**：三字段全空 **26258 行（27.79%）→ 0**；`canonical_company` 空 27404 → 0，
   `company` 空 58744 → 0，`company_name` 空 57248 → 0，`recruiting_unit_raw` 空 7130 → 0；
   腾讯/字节/阿里三家的规范名照旧补齐（腾讯 908、字节跳动、阿里巴巴）。
4. **改名对 35 对 / 11296 行**（上一版在旧快照上是 211 对；**同一份新快照**上旧代码是 94 对）。
   相比旧代码**少了 59 对子品牌/分公司合并（354 行）**；`only_new = 0`，新版没有新增任何合并。
   展示名去重 4016 → **4006**（旧版 3947；去重数更高正是“保留原名”的结果）。
5. 记录数 **94486 不变**；id 集合（排序后 / 原始顺序）sha256 前后完全相同；297 条无 id 行不变。
   第二遍幂等：`filled_total 0`、`would_change_existing 0`、产物字节不变。
6. 全量 `pytest tests/` = **3 failed / 647 passed / 55 skipped**，失败清单与 `feat/collector-next-4`
   基线（3 failed / 640 passed / 55 skipped）**逐条相同**，未新增；647 = 640 + 本次新增 7 条用例。

---

## 2. 站长口径与判定规则（写进表内 basis）

站长 2026-09-19：
1. “类似**网易互娱**这种不需要并入网易，按照原来名称区分开即可。”
2. “**集团和子公司不需要并入集团，直接写明公司名字就好，不要做太复杂。**”

判据：
- **保留**（同一实体的不同写法）：法人全称 → 该法人自己的通用品牌/简称（剥“有限公司/股份有限公司/
  集团有限公司”等法人后缀），例：`腾讯科技（深圳）有限公司 → 腾讯`、`北京小桔科技有限公司 → 滴滴`、
  `大疆创新 → 大疆`、`中国移动通信集团有限公司 → 中国移动`。
- **删除**（不同实体的合并）：把独立品牌/事业群并入母品牌（`网易互娱 → 网易`）；把省分公司/研究院/
  事业部等并入集团（前缀式 `中国移动通信* → 中国移动`）。
- **拿不准保留原名**（第 4 步是默认结局，代码未改）。

---

## 3. 逐条审查（原 19 条 aliases + 3 条 prefixes + 60 条 brands）

### 3.1 删除的映射（5 条）

| 删除项 | 原映射 | 理由 |
|---|---|---|
| alias | 网易互娱 → 网易 | 站长点名：事业群/子品牌，按原名称区分；53 行保留为“网易互娱” |
| alias | 网易互联网 → 网易 | 站长口径同 1；网易的“互联网项目”子品牌，按原名称区分 |
| prefix | 中国移动通信 → 中国移动 | 前缀=“同一集团多个法人主体”的集团化合并；省分公司/在线营销服务中心/研究院/事业部全部保留原名 |
| prefix | 中国联合网络通信 → 中国联通 | 同上；省分公司/研究院/软件研究院/事业部全部保留原名 |
| prefix | 中国邮政 → 中国邮政 | 同上；省分公司保留原名。集团法人 `中国邮政集团有限公司` 已有精确别名，不受影响 |

### 3.2 保留的 aliases（17 条原样 + 2 条新增；共 19 条）

- 原样保留 17 条（同一实体，逐条带 basis）：腾讯科技（深圳）有限公司→腾讯、北京小桔科技有限公司→滴滴、
  大疆创新→大疆、达能（中国）→达能、杭州海康威视数字技术股份有限公司→海康威视、
  用友网络科技股份有限公司→用友网络、科大讯飞股份有限公司→科大讯飞、
  宁德时代新能源科技股份有限公司→宁德时代、比亚迪股份有限公司→比亚迪、
  药明康德新药开发有限公司→药明康德、长城汽车股份有限公司→长城汽车、
  招商局仁和人寿保险股份有限公司→招商局仁和人寿（**保留子公司层**）、
  国家能源投资集团有限责任公司→国家能源集团、北京石晶光电科技股份有限公司→石晶光电、
  中国邮政集团有限公司→中国邮政、中国建设银行股份有限公司→中国建设银行、
  中国银行股份有限公司→中国银行。
- 新增 2 条（替代被删前缀、只收敛集团法人自己）：
  `中国移动通信集团有限公司 → 中国移动`、`中国联合网络通信集团有限公司 → 中国联通`，
  basis 明写“省分公司/事业部/研究院一律保留原名，原前缀式集团合并已按站长口径删除”。

### 3.3 brands（60 条全部保留）

`brands` 只做两件保守的事：① 文本本身就是已知品牌 → 原样返回；② 逐层剥法人后缀后
**正好等于**某个已知品牌 → 该品牌。它不把别的实体“并”进来，也没有“集团化”条目。
60 条 = 50 家硬编码公司规范名 + 字节跳动/阿里巴巴/中国电信 + 7 个集团词条
（中国航天科工集团、中国兵器工业集团、中国兵器装备集团、中国能源建设、中国机械科学研究总院、
中国东方电气集团、招商局集团）——后 7 条都是“**该集团法人自己的**名称剥后缀后命中”，
不是把子公司并入（子公司名如“中国能源建设集团甘肃省电力设计院有限公司”仍保留原名）。

### 3.4 代码改动（`qiuzhao/company_names.py`）

- 删除 `_ORG_TAIL` 正则、`_PREFIXES`、`_PREFIX_KEYS` 与前缀判定分支；`_load()` 只读 `aliases`/`brands`；
  `table_info()` 去掉 `prefixes` 键。判定顺序变为：aliases → 本身是品牌 → 逐层剥法人后缀 → 默认保留原名。
- 结论：**“集团化合并”这条路径在代码层已不存在**，以后即使有人往 JSON 塞 `prefixes` 也不会生效。

---

## 4. 被删除的合并：59 对改名随之消失（354 行）

干跑产物 `rename-diff.csv`（精灵 `C:\mcp-suite-normalize-test\report2\`；
本机副本 `_research/normalize-adjust/精灵报告/report2__rename-diff.csv`）的
「只旧版(已删除的合并)」段共 **59 对 / 354 行**，全部是集团化合并：

| 母品牌 | 条目数 | 行数 | 典型 |
|---|---|---|---|
| 中国联通 | 32 | 127 | 中国联合网络通信有限公司湖北省分公司、研究院、软件研究院、北京网络运营事业部 … |
| 中国移动 | 25 | 103 | 中国移动通信集团北京/浙江/江苏…有限公司、在线营销服务中心、研究院、网络事业部 … |
| 中国邮政 | 1 | 71 | 中国邮政集团有限公司河北省分公司 |
| 网易 | 1 | 53 | 网易互娱 |

（网易互联网那 77 行的 `recruitment_unit` 本来就是“网易”，旧规则下也没产生改名对；
本次仍按站长口径删掉别名，未来若只有 raw=网易互联网 的行将保留原名。）

### 保留的同一实体改名对：35 对 / 11296 行（`renamed-new.csv`）

全部是同一实体：`中国邮政集团有限公司→中国邮政` 2648、`中国联合网络通信集团有限公司→中国联通` 2571、
`中国移动通信集团有限公司→中国移动` 1789、`中国航天科工集团有限公司→中国航天科工集团` 1242、
`腾讯科技（深圳）有限公司→腾讯` 908、`中国能源建设股份有限公司→中国能源建设` 657 …（完整 35 对见 CSV）。
两版都有、逐对行数完全相同（`rename-diff.csv` 的「两版都有」段），说明收窄没有误伤同一实体归一。

---

## 5. 全库干跑（精灵临时目录，未碰正式目录）

### 5.1 快照与三方对照

- 生产库 `C:\mcp-suite-collector\data\jobs.json`（只读复制）：
  **358,682,591 字节，2026-09-19 07:02（06:10 日常跑的产物），94486 条**。
- 三份产物跑在同一份原始快照上，逐位置对比：
  - `before` = 原始快照；
  - `old` = `C:\mcp-suite-normalize-test\code_old`（改前的 `feat/collector-next-4` 代码，含 3 条 prefixes）；
  - `new` = `C:\mcp-suite-normalize-test\code`（本次改动后的代码）。
- 说明：干跑代码目录的底是**精灵现役 k 版** `qiuzhao`（平台配置 893 个规范名），与上一版收据同一口径；
  正式上线时平台配置来自 `20260919g`（1019 行），多出的都是配置公司名本身，探针已验证它们是规范化的不动点。

### 5.2 三个字段空值率 前 → 后

| 字段 | 前 | 前占比 | 后(new) | 后(old) |
|---|---|---|---|---|
| `canonical_company` | 27404 | 29.00% | **0** | 0 |
| `company` | 58744 | 62.17% | **0** | 0 |
| `company_name` | 57248 | 60.59% | **0** | 0 |
| 三字段全空 | 26258 | **27.79%** | **0** | 0 |
| `recruiting_unit_raw` | 7130 | 7.55% | **0** | 0 |
| `recruitment_unit` | 0 | 0% | 0 | 0 |

### 5.3 展示名去重数 前 → 后

| 口径 | 前 | new | old |
|---|---|---|---|
| 展示名（`canonical_company or recruitment_unit or company`，= 线上 MCP 口径） | 4016 | **4006** | 3947 |
| `canonical_company` / `company` / `company_name` 去重 | 未单列 | 4006 | 3947 |
| `recruitment_unit` 去重 | 4916 | 4916 | 4916 |
| `recruiting_unit_raw` 去重 | 未单列 | 5425 | 5425 |

新版比旧版多留 59 组独立名称（4006 > 3947），正是“按原名称区分开”的直接体现。

### 5.4 记录数与 id 集合

- 条数：**94486 → 94486 → 94486**（before/new/old）。
- 排序后 id 集合 sha256：`996df897701991d9f7d63eb311f741d90ca2be28082dd5a6bdd866c3993c430e`
  前/新/旧完全相同；原始顺序 sha256 也相同（`9865211674f9e97178ca7828a0be301222239fd0f5858ea17dd765a8f5666f93`）；
  `same_order_ids True`；297 条无 id 行前后一致。

### 5.5 改名对

| 版本 | 改名对 | 涉及行 |
|---|---|---|
| 上一版（旧快照 93519 条） | 211 | 11629 |
| 旧代码（本快照 94486 条） | 94 | 11650 |
| **本次新版（本快照）** | **35** | **11296** |

`new` 与 `old` 展示名不同的行数 = **354**，恰好等于被删除的 59 对合并的行数；新增改名对 **0**。

### 5.6 幂等、耗时

| 项 | new | old |
|---|---|---|
| 第一遍耗时 | 31.9 s | 31.9 s |
| 第二遍耗时 | 33.05 s | 33.16 s |
| `filled_total` 第一遍 | 150830 | 150832 |
| `would_change_existing` 第一遍 | **7** | 9 |
| 第二遍 `filled_total` / `would_change_existing` | 0 / 0 | 0 / 0 |
| 两遍产物 sha256 | 相同（字节不变） | 相同 |

峰值内存：精灵 venv 的 `psapi.GetProcessMemoryInfo` / `K32GetProcessMemoryInfo` 都返回 0（取不到），
本次未测到内存；上一版同规模干跑实测 2234.8 MB，`windows_collector.py` 给 normalize 的超时 1800 s，余量充足。

### 5.7 `would_change_existing = 7` 明细（全部是预期的同一实体归一）

| 行号 | 字段 | 前 | 后 |
|---|---|---|---|
| 16665-16667（`mokahr_…`） | company | 兆易创新科技集团股份有限公司 | 兆易创新 |
| 17981-17983（`zhiye_iflytek_…`） | company | 科大讯飞股份有限公司 | 科大讯飞 |
| 17999（`zhiye_keyence_390836746`） | company | 基恩士（中国）有限公司 | 基恩士 |

旧版是 9 条，多出的 2 条正是被删除的合并
（`中国联合网络通信有限公司湖北省/新疆分公司 → 中国联通`）。
`would_change_existing` 全仓无代码消费（`windows_collector.py` 只看 normalize 退出码），不构成拦截。

---

## 6. 单测

`tests/test_company_names.py`：42 → **49 条**。改动：
- `test_alias_hits` 去掉“网易互娱/网易互联网 → 网易”两条；
- 新增 `test_group_legal_entity_itself_is_normalized`（集团法人→品牌）；
- 新增 `test_group_and_subbrands_are_not_merged`（11 个反例：网易互娱、网易互联网、网易有道、
  中国移动/联通/邮政的省分公司/研究院/软件研究院/事业部）；
- 新增 `test_alias_table_has_no_group_prefix_merges`（表里无 prefixes、代码无 `_PREFIXES/_PREFIX_KEYS`）；
- 自检用例适配 `table_info()` 不再有 `prefixes` 键。

全量回归（主仓 venv，`PYTHONPATH=<worktree>`，`pytest tests/ -q`）：

| 版本 | 结果 |
|---|---|
| 基线 `feat/collector-next-4` `82af4f72`（交接文档记录） | 3 failed / 640 passed / 55 skipped |
| 本分支 | **3 failed / 647 passed / 55 skipped** |

失败清单逐条相同：`test_core::test_role_cohort_and_campaign_title_bases`、
`test_p1_pipeline::P1Tests::test_timeout_publishes_only_validated_partial_checkpoint`、
`test_schema::test_enum_check_fails_when_data_drifts` —— 与交接文档记录的 3 条既有失败一致，**未新增**。

零网络探针 `pipeline-watch/collector-next4-probe.py`：**PROBE OK**，`names_normalization_would_rewrite = []`
（1019 条配置公司名仍是规范化不动点），`normalization_table = {aliases: 19, brands: 1080, platform_names: 1020}`。

---

## 7. 为什么 211 → 35 比 94 → 35 降得更多（口径说明，不是数据变化）

上一版收据里的 211 对，包含约 130 对 `* → Zuora` 的伪影：该版干跑脚本用
`{id: 展示名}` 建 before 表，**297 条无 id 的行全部落在同一个空串键上互相覆盖**，
对比时这些行全部被当成“从最后一行的展示名改名而来”。
本次改用**逐位置对比**（与 `would_change_existing` 同一算法），该伪影消失：
同一份新快照上旧代码只有 94 对，新版 35 对。
所以真实降幅是 **94 → 35（少 59 对合并）**，与 211 的差是测量方法修正。

---

## 8. 部署件

`pipeline-watch/deploy-artifacts/20260919i/`（**叠加在 20260919g 之上，只替换 g 里的同名文件**）：

| 文件 | sha256 |
|---|---|
| `company_names.py` | `ee0943560d6ed0523b4bd70f6363b529e8190b60333fad2fc7a6678ec4198851` |
| `company_aliases.json` | `ae311e93b754b5f76372f7668aa1076fd6666e79c51ca45ff2113b7642539e6f` |

`SHA256SUMS.txt` 两个文件 `shasum -a 256 -c` 全 OK，且与分支源码逐字节一致；
`DEPLOY-NOTES.md` 写明“用本目录两个文件替换 g 里的同名文件”。
本部署件不含 `20260919g` 的其他 24 个文件，也不含 `normalize.py`（无改动）。

## 9. 待决策 / 遗留

1. **是否部署**：本次只是把规范化收窄到站长口径；部署要 Max 说“上线”，并叠在 `20260919g` 之上。
2. `recruitment_unit` 是否升级为用人单位层——站长已明确“不要做太复杂”，本项**从待办去掉**。
3. 集团/子公司归属表——站长已明确不要，**从待办去掉**。
4. 别名表以后要不要继续扩：仍只收“同一实体”的写法，且每条带 basis；`brands` 的保守命中逻辑不变。

## 10. 产物清单

| 产物 | 位置 |
|---|---|
| 代码 | 分支 `feat/normalize-adjust`：`qiuzhao/company_names.py`、`qiuzhao/data/company_aliases.json`、`tests/test_company_names.py` |
| 部署件 | `pipeline-watch/deploy-artifacts/20260919i/`（2 运行时文件 + `SHA256SUMS.txt` + `DEPLOY-NOTES.md`） |
| 干跑脚本 | `_research/normalize-adjust/s1_before.py`、`s2_normalize.py`、`s3_compare.py`、`s4_changed.py`、`fetch_report.py`、`push_files.py` |
| 干跑原始报告 | 精灵 `C:\mcp-suite-normalize-test\report2\`；本机副本 `_research/normalize-adjust/精灵报告/`（before.json、compare.json、renamed-new.csv、renamed-old.csv、rename-diff.csv、top100-before-after.csv、new/old stats.json） |

## 11. 未做的事

未部署、未覆盖精灵正式目录（`C:\mcp-suite-collector` 全程只读）、未碰阿里云、未调飞书任何接口、
未读取/打印任何令牌、未登录、未绕过验证码/签名、未 push、未合并 main、未终止任何进程、未使用 `git stash`。
