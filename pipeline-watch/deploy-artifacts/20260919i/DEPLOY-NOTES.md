# 20260919i — 公司名规范化收窄（只补空，不合并品牌）

本部署件**叠加在 `20260919g` 之上**，只含本次改动的两个文件，用于**替换 g 里的同名文件**，
其余 24 个 g 文件不动。

| 本目录文件 | 精灵目标路径 | 说明 |
|---|---|---|
| `company_names.py` | `qiuzhao\company_names.py` | 删除 `prefixes` 前缀式“集团化合并”逻辑 |
| `company_aliases.json` | `qiuzhao\data\company_aliases.json` | 删 2 条品牌合并别名 + 3 条前缀，新增 2 条集团法人精确别名 |

## 站长口径（2026-09-19）
1. “类似网易互娱这种不需要并入网易，按照原来名称区分开即可。”
2. “集团和子公司不需要并入集团，直接写明公司名字就好，不要做太复杂。”

## 相对 g 的行为变化
- **不再把子品牌/子公司/省分公司/事业部并入母公司**：网易互娱、网易互联网、中国移动通信集团{省}有限公司、
  中国联合网络通信有限公司{省}分公司/研究院/软件研究院/事业部、中国邮政集团有限公司{省}分公司 等
  一律保留原名。
- 删除 3 条前缀式集团合并（原 `prefixes`：中国移动通信 / 中国联合网络通信 / 中国邮政）；
  集团法人自己改用精确别名：`中国移动通信集团有限公司 → 中国移动`、`中国联合网络通信集团有限公司 → 中国联通`。
- “补空”不变：`canonical_company` / `company` / `company_name` 仍照旧补齐（本次干跑 27404 条 canonical 空 → 0）。

## 部署方式
用 `20260919g` 的 26 文件清单把 g 全部覆盖后，再用本目录两个文件覆盖同名文件
（等价于“用 20260919i 替换 g 里的同名文件”），然后 `py_compile` + import 闭包检查。
`SHA256SUMS.txt` 只校验本目录两个文件。

## 本部署件未做的事
未部署、未 SSH 写精灵正式目录（干跑只在 `C:\mcp-suite-normalize-test\`）、未碰阿里云、未调飞书、
未读取/打印令牌、未 push、未合并 main。
